[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "teiknical"
description = "Teiko Technical Exam"
authors = [
    {name = "Kevin Fox", email = "foxkevin@protonmail.com"},
]
readme = "README.md"
requires-python = ">=3.12"
version = "0.1.0"
